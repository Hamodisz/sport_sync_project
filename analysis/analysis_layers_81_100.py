def apply_layers_81_100(text):
    traits = []

    # 81. الجسد كحمل ثقيل
    if any(w in text for w in ["جسمي يتعبني", "أحس بثقل", "أبغى أرتاح من نفسي"]):
        traits.append("يرى جسده كعبء، ويحتاج رياضة خفيفة تُعيد له العلاقة الإيجابية بجسده.")

    # 82. الخوف من الحكم
    if any(w in text for w in ["ينتقدوني", "أخاف أتحطم", "ما أحب أحد يراقبني"]):
        traits.append("يحمل قلقًا من تقييم الآخرين، ويحتاج بيئة آمنة للتجربة بدون مقارنة.")

    # 83. الميل للغموض
    if any(w in text for w in ["ما أحب أوضح", "أحس أشياء بس ما أقول", "أنا غريب"]):
        traits.append("يحب الغموض والتعبير غير المباشر، وقد تناسبه رياضات حركية رمزية.")

    # 84. العيش في الخيال أكثر من الواقع
    if any(w in text for w in ["أتخيل أكثر من أعيش", "براسي أشياء كثيرة", "الخيال أقوى من الواقع"]):
        traits.append("عقله يعيش في عالم موازٍ، ويحتاج رياضة تمزج بين الواقع والخيال.")

    # 85. الحس بالفوضى الداخلية
    if any(w in text for w in ["كل شي متداخل", "أفكاري كثير", "ما أعرف أرتب نفسي"]):
        traits.append("يعيش صراعًا داخليًا، ويحتاج رياضة تنظم الإيقاع الداخلي وتفرغه.")

    # 86. الخوف من البداية
    if any(w in text for w in ["أخاف أبدأ", "أجل باستمرار", "ما أقدر أبدأ من نفسي"]):
        traits.append("يحتاج محفزًا خارجيًا وإحساسًا بالأمان لبدء أي تجربة جديدة.")

    # 87. الحس بالانفصال عن الجسد
    if any(w in text for w in ["ما أحس بجسمي", "أتحرك بس ما أركز", "أنا مفصول عن إحساسي"]):
        traits.append("منفصل عن جسده وقد تناسبه رياضات تعيد له الوعي الحركي والشعوري.")

    # 88. الهوس بالإنتاجية
    if any(w in text for w in ["ما أضيع وقت", "أبغى كل دقيقة مفيدة", "أحسب كل خطوة"]):
        traits.append("ينظر للحركة كواجب إنتاجي، ويحتاج رياضة تعيد له الشعور الحر.")

    # 89. الميل للتجربة المتطرفة
    if any(w in text for w in ["أبغى شي يكسرني", "ودي أعيش أقصى شي", "أبغى أتفجر"]):
        traits.append("يتجه للتجارب القصوى، ويبحث عن رياضة تهزه بالكامل.")

    # 90. الشعور بالسطحية في كل شيء
    if any(w in text for w in ["كل شي صار ممل", "ما في عمق", "أحس كل شي فاضي"]):
        traits.append("يبحث عن العمق، وقد تناسبه رياضات ذات بعد فلسفي أو طقوسي.")

    # 91. الميل للاندماج الصوفي
    if any(w in text for w in ["أدخل جو", "أنفصل عن الواقع", "أحس إني أذوب"]):
        traits.append("يريد تجربة روحية داخل الجسد، وقد تناسبه رياضات استغراقية أو طقسية.")

    # 92. التكرار كوسيلة للثبات
    if any(w in text for w in ["أحب أكرر", "أحب النمط", "أحتاج شي ثابت"]):
        traits.append("يرتاح للتكرار، وقد تناسبه رياضات روتينية لها طقوس مريحة.")

    # 93. العلاقة بالجسد كهوية مضادة
    if any(w in text for w in ["أبغى أثبت شي", "أبغى أتغير تمامًا", "أبغى أطلع من نفسي"]):
        traits.append("يحاول تغيير صورته الذاتية عبر الجسد، ويحتاج رياضة داعمة لهويته الجديدة.")

    # 94. الخوف من الفراغ
    if any(w in text for w in ["أخاف أقعد ساكت", "الفراغ يقتلني", "أحتاج شي يشغلني"]):
        traits.append("يتحرك بدافع الهروب من الفراغ، ويحتاج رياضة تُشعره بالامتلاء لا التشتت.")

    # 95. العلاقة الحسية الدقيقة
    if any(w in text for w in ["أحس بأدق التفاصيل", "أنتبه للوزن", "أراقب تنفسي"]):
        traits.append("يتفاعل مع الحواس بدقة، وقد تناسبه رياضات واعية حركيًا وجسديًا.")

    # 96. الحاجة لتفريغ صوتي أو تعبيري
    if any(w in text for w in ["أصرخ", "أغني", "أطلق صوت", "أبغى أتنفس بصوت"]):
        traits.append("يحتاج إلى تفريغ تعبيري، وقد تناسبه رياضات فيها صوت أو تنفس مفتوح.")

    # 97. الحس بالتحكم الزائد
    if any(w in text for w in ["أخطط لكل شي", "أخاف أفقد السيطرة", "كل شي لازم يكون مضبوط"]):
        traits.append("يميل للسيطرة الكاملة، ويحتاج رياضة تتيح له الاستسلام التدريجي.")

    # 98. ضعف التواصل الجسدي مع الآخرين
    if any(w in text for w in ["ما أحب أحد يلمسني", "ما أرتاح في التلامس", "أخاف أواجه جسديًا"]):
        traits.append("منفصل جسديًا اجتماعيًا، ويحتاج رياضة تعزز ثقته في التواصل البدني.")

    # 99. الخجل من الحركة أمام الناس
    if any(w in text for w in ["أستحي أتحرك قدامهم", "أتوتر لما يشوفوني", "أنحرج"]):
        traits.append("يخجل من أداء الجسد علنًا، ويحتاج بيئة مغلقة وداعمة للبداية.")

    # 100. الحس باللاجدوى
    if any(w in text for w in ["وش الفايدة؟", "ما له معنى", "كل شي يرجع من أول"]):
        traits.append("يعاني من إحساس باللاجدوى، ويحتاج رياضة تبني له سببًا حقيقيًا للحركة.")

    return traits