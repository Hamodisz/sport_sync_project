def apply_layers_1_40(text):
    traits = []

    # 1. المخاطرة والأدرينالين
    if any(w in text for w in ["مخاطرة", "مجازفة", "اندفاع", "أدرينالين", "قفز", "سرعة", "قتال"]):
        traits.append("يميل إلى المجازفة ويبحث عن تجارب مشحونة بالأدرينالين.")

    # 2. السلام الداخلي
    if any(w in text for w in ["هدوء", "استرخاء", "تأمل", "بطء", "تنفس", "راحة"]):
        traits.append("يبحث عن السلام الداخلي والتوازن من خلال الحركة.")

    # 3. الانضباط والتحكم
    if any(w in text for w in ["تحكم", "انضباط", "روتين", "صرامة", "اتزان", "ثبات"]):
        traits.append("يحب الانضباط ويتجه نحو الرياضات التي تعتمد على الدقة والتحكم.")

    # 4. الإبداع الحركي
    if any(w in text for w in ["إبداع", "حُر", "تعبير", "ارتجال", "رقص", "رسم", "فن"]):
        traits.append("يرى الرياضة كأداة للتعبير عن الذات والإبداع.")

    # 5. الذكاء التحليلي
    if any(w in text for w in ["تحليل", "تفكير", "عقل", "استراتيجية", "ذكاء"]):
        traits.append("يميل للرياضات التي تتطلب تفكيرًا وتحليلًا منطقيًا.")

    # 6. التنفيس العاطفي
    if any(w in text for w in ["غضب", "تفريغ", "انفجار", "توتر", "ضغط"]):
        traits.append("يستخدم الرياضة كوسيلة لتفريغ العواطف والانفعالات.")

    # 7. العزلة والداخلية
    if any(w in text for w in ["لوحدي", "أنعزل", "أكره الزحمة", "فردي", "ما أحب أحد يراقبني"]):
        traits.append("يميل إلى الأنشطة الفردية ويجد راحته في العزلة.")

    # 8. الاجتماعية والتشارك
    if any(w in text for w in ["فريق", "لعب جماعي", "أصدقاء", "جماعة", "تعاون"]):
        traits.append("يستمد طاقته من التفاعل الاجتماعي ويحب الرياضات الجماعية.")

    # 9. الكمالية والحكم على الذات
    if any(w in text for w in ["لازم أتقن", "ما أرضى بأقل", "أجلد نفسي", "أقارن", "أحاسب نفسي"]):
        traits.append("يميل للكمالية ويبحث عن الإنجاز بدافع داخلي قوي.")

    # 10. الجسد كمسرح
    if any(w in text for w in ["أحب أُظهر", "الجسم يعبر", "كل حركة تقول شيء"]):
        traits.append("يرى الجسد كوسيلة درامية لسرد الذات والانفعالات.")

    # 11. المتعة قبل الهدف
    if any(w in text for w in ["أبغى أستمتع", "أبغى ألعب", "أحب الفوضى", "الجدية تقتلني"]):
        traits.append("يريد تجربة ممتعة وحرة بعيدًا عن الضغط والإنجاز.")

    # 12. هدف واضح وانتصار
    if any(w in text for w in ["أبغى أفوز", "أبغى أوصل", "أبغى أنجز", "النتيجة تهمني"]):
        traits.append("تحفزه النتيجة ويبحث عن الإنجاز والبطولة.")

    # 13. الحسية الجسدية
    if any(w in text for w in ["أحس بجسمي", "ألم", "تعرّق", "نفس", "عضلات", "ملمس"]):
        traits.append("يتواصل مع جسده بعمق ويهتم بالشعور الحركي الحسي.")

    # 14. البصرية والانتباه
    if any(w in text for w in ["أحب أشوف", "أناظر", "ألوان", "ألاحظ التفاصيل"]):
        traits.append("شخص بصري، يتفاعل مع المشهد والشكل والحركة الظاهرة.")

    # 15. السمعي والإيقاعي
    if any(w in text for w in ["صوت", "إيقاع", "نغمة", "لحن", "أحس بالإيقاع"]):
        traits.append("يرتبط بالحركة من خلال الإيقاع والنغمة.")

    # 16. الواقعية والانضباط التقليدي
    if any(w in text for w in ["رياضة معروفة", "الطريقة الصح", "التقاليد", "النظام"]):
        traits.append("يرتاح للأنظمة الرياضية المألوفة والمجربة.")

    # 17. الخيال والهروب
    if any(w in text for w in ["أهرب", "أتخيل", "أحلم", "أخلق عوالم"]):
        traits.append("يستخدم الرياضة كمدخل للخيال والهروب من الواقع.")

    # 18. الحس التنافسي العالي
    if any(w in text for w in ["أنافس", "أغار", "أبي أكون الأفضل"]):
        traits.append("شخصية تنافسية تغذيها المقارنة والإنجاز.")

    # 19. الراحة فوق الإنجاز
    if any(w in text for w in ["ما أحب التعب", "أبغى بس أتحرك", "أرتاح"]):
        traits.append("يفضل الراحة والتدفق الحر على الإنجاز المكثف.")

    # 20. القلق من الفشل
    if any(w in text for w in ["أخاف أفشل", "أتردد", "ما أقدر أكمّل", "أتوتر بسرعة"]):
        traits.append("يحمل قلقًا من الفشل ويحتاج رياضة تمنحه الأمان قبل الضغط.")

    # 21. حب الأداء المسرحي
    if any(w in text for w in ["جمهور", "أُبهر", "أُدهش", "أحب أعرض"]):
        traits.append("يتغذى على الظهور ويحب الرياضات التي فيها عرض وتأثير بصري.")

    # 22. الدوافع الداخلية العميقة
    if any(w in text for w in ["ما أدري ليه بس أحب", "ما أقدر أوصف", "أحسها داخليًا"]):
        traits.append("يمارس الرياضة بناءً على شعور داخلي غامض وصادق.")

    # 23. ربط الرياضة بالهوية
    if any(w in text for w in ["أنا كذا", "هذي أنا", "تمثلني", "مو بس رياضة"]):
        traits.append("يرى أن الرياضة تمثل جزءًا من هويته، وليست مجرد أداة.")

    # 24. الإنهاك الذهني والبحث عن تفريغ
    if any(w in text for w in ["أفكر كثير", "أحس راسي مشغول", "ودي أفرّغ"]):
        traits.append("تساعده الرياضة على تفريغ التوتر الذهني والعودة للبساطة.")

    # 25. السيطرة مقابل الفوضى
    if any(w in text for w in ["كل شي عندي لازم يكون مضبوط", "أكره العشوائية", "أحب أتحكم"]):
        traits.append("يميل للرياضات التي تعطيه شعورًا بالتحكم وتنظيم الفوضى.")

    # 26. الخضوع للمجهول
    if any(w in text for w in ["أحب أرتجل", "أخلي جسمي يقرر", "ما أخطط"]):
        traits.append("ينجذب للرياضات التي تترك مجالًا للارتجال والتدفق العفوي.")

    # 27. التحفيز البصري الخيالي
    if any(w in text for w in ["ألعاب", "واقع افتراضي", "أنمي", "كوميكس"]):
        traits.append("يتأثر بالعوالم الخيالية وقد تناسبه رياضات تقنية أو تجريبية.")

    # 28. الحس بالخطر والحياة
    if any(w in text for w in ["أحس بالحياة", "أقرب للموت", "قلبي ينبض"]):
        traits.append("يحب الشعور بالحياة في حافة الخطر، ويبحث عن الإثارة الوجودية.")

    # 29. حب التفاصيل الميكانيكية
    if any(w in text for w in ["تفاصيل", "آلية", "تقنية", "كيف أشتغل جسمي"]):
        traits.append("يفكر في الجسد كآلة ويريد فهم كيف يعمل بدقة.")

    # 30. البحث عن الانتماء
    if any(w in text for w in ["أبغى أنتمي", "أحس بوحدة", "أدور مجتمع"]):
        traits.append("يبحث عن بيئة تمنحه شعورًا بالانتماء من خلال النشاط الرياضي.")

    # 31. الإنغلاق والانكفاء الداخلي
    if any(w in text for w in ["أكتم", "ما أقول", "أحط كل شيء داخلي"]):
        traits.append("يحمل مشاعر مكبوتة وقد يحتاج لرياضة تساعده في التفريغ التدريجي.")

    # 32. استخدام الألم كدافع
    if any(w in text for w in ["الألم يخليني أستمر", "أحب أحس بالألم", "أتمرن لما أتألم"]):
        traits.append("يرى في الألم محفزًا حقيقيًا للوجود والحركة.")

    # 33. اللا مبالاة بالهدف
    if any(w in text for w in ["ما عندي هدف", "أشوف وش يصير", "ما يهمني أوصل"]):
        traits.append("يبحث عن التجربة نفسها لا عن النهاية.")

    # 34. الحاجة للمرونة والتحرر
    if any(w in text for w in ["أكره الروتين", "أحب أغير", "أحب أتنقل كثير"]):
        traits.append("ينفر من الثبات ويحتاج إلى رياضات مرنة وديناميكية.")

    # 35. الدافع الأخلاقي أو الروحي
    if any(w in text for w in ["أقرب لله", "أصفى", "أرتاح داخليًا", "رياضة روحانية"]):
        traits.append("يربط الرياضة بالقيم العليا والجانب الروحي في ذاته.")

    # 36. الحس بالزمن والموت
    if any(w in text for w in ["الوقت يركض", "أحس أعمارنا تروح", "أبغى أعيش قبل ما أفوت"]):
        traits.append("يتحرك بدافع الزمن المحدود، ويرى الحركة كنجاة وجودية.")

    # 37. الميل إلى التعليم والتأثير
    if any(w in text for w in ["أحب أعلم", "أحب أشرح", "أبغى أكون قدوة"]):
        traits.append("قد يتجه لرياضات تعليمية أو فيها دور قيادي/تمثيلي.")

    # 38. الخوف من نظرة الآخرين
    if any(w in text for w in ["أتوتر لو أحد شافني", "أخاف أغلط قدام أحد"]):
        traits.append("يحتاج إلى بيئة آمنة نفسيًا قبل أن يواجه التحدي.")

    # 39. المقاومة للسلطة
    if any(w in text for w in ["ما أحب أحد يقول لي", "أكره الأوامر", "أقرر لحالي"]):
        traits.append("يتفاعل أفضل في بيئات حرة ولا يناسبه النظام الصارم.")

    # 40. التمرد على القواعد
    if any(w in text for w in ["أغير القوانين", "أحب أعدل", "أسويها بطريقتي"]):
        traits.append("يحب اختراع أسلوبه الخاص، وقد يبتكر رياضته بنفسه.")

    return traits