def apply_layers_101_141(text):
    traits = []

    # 101. وجود صدمة سابقة
    if any(w in text for w in ["تجربة قاسية", "ما أنساها", "خسرت شيء", "صعب أتكلم عنه"]):
        traits.append("مرّ بتجربة صادمة وقد يحتاج رياضة تعيد له الأمان والسيطرة.")

    # 102. الخوف من المواجهة
    if any(w in text for w in ["ما أقدر أواجه", "أهرب دايم", "أتفادى المواجهة"]):
        traits.append("يتفادى الصدام، ويحتاج رياضة تمنحه ثقة تدريجية في ردود فعله.")

    # 103. التعلق بأذى سابق
    if any(w in text for w in ["تأذيت", "ما غفرت", "باقي فيني أثر"]):
        traits.append("يحمل أثرًا نفسيًا قديمًا، ويحتاج تفريغًا آمنًا ومرنًا عبر الحركة.")

    # 104. السخرية كآلية دفاع
    if any(w in text for w in ["أمزح كثير", "أضحك عشان أهرب", "ما أحب أتكلم جد"]):
        traits.append("يستخدم الفكاهة كقناع دفاعي، ويحتاج رياضة تحترم عمقه دون ضغط.")

    # 105. الصمت كوسيلة بقاء
    if any(w in text for w in ["أفضل السكوت", "ما أتكلم عني", "أكتم كثير"]):
        traits.append("يتخذ الصمت كآلية حماية، ويحتاج رياضة تساعده على التعبير دون كلام.")

    # 106. الغضب المقنّع
    if any(w in text for w in ["أعصب فجأة", "أكتم لين أنفجر", "ما أحد يفهمني"]):
        traits.append("يخزن الغضب داخليًا، وقد تناسبه رياضات تفريغ قوية وآمنة.")

    # 107. الشعور بالنقص
    if any(w in text for w in ["أحس أقل", "ما أنجزت شيء", "أنا دايم متأخر"]):
        traits.append("يشعر بالنقص، ويحتاج تجربة رياضية تعزز قيمته الذاتية بغض النظر عن الإنجاز.")

    # 108. مشاعر غير مصرح بها
    if any(w in text for w in ["أخاف أقول", "ما أحد يصدقني", "أكتم رغباتي"]):
        traits.append("يمر بمشاعر مكبوتة، ويحتاج مساحة آمنة غير حكمية للحركة.")

    # 109. التعلّق المفرط
    if any(w in text for w in ["ما أقدر أترك شيء", "أتمسك بأي أحد", "أحس بالفراغ بسرعة"]):
        traits.append("يتعلق بالأشياء/الأشخاص بقوة، ويحتاج رياضة تعزز استقلاله النفسي.")

    # 110. جلد الذات المتراكم
    if any(w in text for w in ["كل شي غلطتي", "أنا السبب", "أحاسب نفسي كثير"]):
        traits.append("يمارس جلدًا ذاتيًا مزمنًا، ويحتاج رياضة تشجعه على الرحمة تجاه ذاته.")

    # 111. التصالح مع الظل
    if any(w in text for w in ["عرفت حقيقتي", "قبلت عيوبي", "واجهت ألمي"]):
        traits.append("بدأ يواجه ذاته بصدق، ويحتاج رياضة تساعده على إعادة البناء بهدوء.")

    # 112. التعلق بالمثالية
    if any(w in text for w in ["لازم أكون الأفضل", "ما أرضى بالأخطاء", "أبغى أكمل كل شي"]):
        traits.append("مثالي متعب، ويحتاج رياضة تسمح له بالتطور لا بالكمال.")

    # 113. الكبت الجنسي أو الحركي
    if any(w in text for w in ["أخجل من جسمي", "أحس بشي ما أقدر أقوله", "في طاقة داخلية مدفونة"]):
        traits.append("يخزن طاقة داخلية مكبوتة، وقد تناسبه رياضات تحرر الحواس وتبني الثقة.")

    # 114. العيش في دور الضحية
    if any(w in text for w in ["الكل ضدي", "أنا دايم المظلوم", "ما أحد يفهمني"]):
        traits.append("يحمل شعورًا بالظلم المستمر، ويحتاج رياضة تعيد له agency وقوة اتخاذ القرار.")

    # 115. الشعور بالذنب المستمر
    if any(w in text for w in ["أندم كثير", "ما أقدر أغفر لنفسي", "أحسني السبب في كل شي"]):
        traits.append("يتحرك من منطلق الذنب، ويحتاج رياضة تخلق له فرصة جديدة للصفح والتوازن.")

    # 116. فقدان الشعور بالحياة
    if any(w in text for w in ["أحس ميت", "ما في شي يحركني", "ما لي طعم"]):
        traits.append("يعاني من خدر داخلي، ويحتاج رياضة تعيد له النبض الأولي للحياة.")

    # 117. صراع الهوية
    if any(w in text for w in ["ما أعرف من أنا", "أدوّر نفسي", "تعبت من التناقض"]):
        traits.append("يمر بارتباك في الهوية، ويحتاج رياضة تساعده في اكتشاف نفسه بدون فرض.")

    # 118. الصمت القهري
    if any(w in text for w in ["ما أنطق", "ما أعرف أعبّر", "الصمت يحميني"]):
        traits.append("استخدم الصمت لحماية نفسه طويلًا، ويحتاج رياضة تنقله تدريجيًا للتعبير.")

    # 119. التكرار القهري
    if any(w in text for w in ["أعيد نفس الخطأ", "كل مرة أبدأ وأنهار", "نمط يتكرر"]):
        traits.append("يعيش في دورة نفسية متكررة، ويحتاج رياضة تكسر الدائرة."

)

    # 120. فقدان السيطرة
    if any(w in text for w in ["أفقد أعصابي", "أتهور", "أضيع كل شي بسرعة"]):
        traits.append("يفقد التوازن بسهولة، ويحتاج رياضة تعزز الاستقرار والتحكم باللحظة.")

    # 121. رفض الجسد
    if any(w in text for w in ["ما أحب شكلي", "أتقرف من جسمي", "أكره أطالع نفسي"]):
        traits.append("يرفض جسده، ويحتاج رياضة تصنع علاقة حب لا مقارنة.")

    # 122. إحساس بعبثية الرياضة
    if any(w in text for w in ["وش الفايدة من التمرين؟", "أتمرن وبس", "أحسها ما تضيف لي شي"]):
        traits.append("يشعر بفراغ في المعنى، ويحتاج رياضة تعطيه بُعدًا روحيًا أو فلسفيًا.")

    # 123. الحس بأن الجهد بلا تقدير
    if any(w in text for w in ["أتعب وما أحد يلاحظ", "ما حد يقدّر", "أشتغل بصمت"]):
        traits.append("يريد الاعتراف، ويحتاج رياضة تمنحه شعورًا داخليًا بالإنجاز بغض النظر عن الجمهور.")

    # 124. التماهي مع القوة كقناع
    if any(w in text for w in ["أنا قوي", "ما أحب أضعف", "القوة كل شي"]):
        traits.append("يتماهي مع القوة كدرع نفسي، ويحتاج رياضة تعلّمه أن الضعف ليس تهديدًا.")

    # 125. رفض الطفولة
    if any(w in text for w in ["كبرت بدري", "ما عشت طفولتي", "كل شي كان مسؤولية"]):
        traits.append("فقد مرحلة البراءة، ويحتاج رياضة تعيد له المرح والحرية.")

    # 126. مشاعر مهجورة
    if any(w in text for w in ["ما أحد سأل عني", "دايم أنسى", "ما لي مكان"]):
        traits.append("يحمل مشاعر إهمال، ويحتاج رياضة تمنحه الانتباه والرعاية.")

    # 127. الحس بالذوبان في الآخرين
    if any(w in text for w in ["أنسى نفسي مع الناس", "أقلد كثير", "ما لي رأي خاص"]):
        traits.append("يفقد هويته في المجموع، ويحتاج رياضة فردية تعزز استقلاله.")

    # 128. الخوف من التلاشي
    if any(w in text for w in ["أخاف أنتهي", "أضيع بسرعة", "ما حد بيذكرني"]):
        traits.append("يحمل رعبًا من التلاشي، ويحتاج رياضة تعزز أثره ووجوده.")

    # 129. التقديس المبالغ للصمت
    if any(w in text for w in ["الصمت أجمل", "الكلام يخرب", "ما أحتاج أشرح"]):
        traits.append("قدّس الصمت لدرجة كتم كل شيء، ويحتاج رياضة تعبّر عنه بدون كلام.")

    # 130. النزعة للتدمير الذاتي
    if any(w in text for w in ["أهلك نفسي", "ما أرحم جسمي", "أكسرني عمدا"]):
        traits.append("يمارس سلوكًا تدميريًا، ويحتاج رياضة تعيد له احترام الجسد.")

    # 131. الشعور بأن لا أحد يفهمه
    if any(w in text for w in ["كلهم يسيئون فهمي", "أنا غريب", "أشرح كثير وما أحد يستوعب"]):
        traits.append("يعاني من عزلة فهم، ويحتاج رياضة تُترجم مشاعره دون شرح.")

    # 132. البحث عن بداية جديدة
    if any(w in text for w in ["أبغى أبدأ من الصفر", "أبغى أرجع أخلق نفسي"]):
        traits.append("يبحث عن ولادة جديدة، ويحتاج رياضة تُمكّنه من إعادة بناء ذاته.")

    # 133. الانسلاخ من الماضي
    if any(w in text for w in ["خلاص ما أبغى أرجع", "أنسى كل اللي صار", "كأن ذيك مو أنا"]):
        traits.append("يريد التحرر من ماضيه، ويحتاج رياضة لا تُذكّره بل تفتح له هوية جديدة.")

    # 134. الشعور بالوحدة الوجودية
    if any(w in text for w in ["ما أحد معي", "أواجه كل شي لحالي", "أنا لحالي بعالمي"]):
        traits.append("يحمل وحدة داخلية، ويحتاج رياضة تشعره بأنه ليس وحده فعلًا.")

    # 135. الرغبة في الإنقاذ
    if any(w in text for w in ["أحتاج أحد ينقذني", "أنتظر شيء يغيرني", "ودي أحد يمد يده"]):
        traits.append("ينتظر الخلاص، ويحتاج رياضة تُفعّل فيه الإرادة لا التبعية.")

    # 136. التماهي مع الألم
    if any(w in text for w in ["الألم صديقي", "تعودت على الوجع", "الألم صار عادي"]):
        traits.append("يحيا في الألم، ويحتاج رياضة تعلّمه الفرح كوسيلة للحياة لا الهروب منها.")

    # 137. الذاكرة الجسدية المؤلمة
    if any(w in text for w in ["جسمي يتذكر", "المكان يرجّع لي مشهد", "أحسها بجسدي"]):
        traits.append("يحمل في جسده ذاكرة ألم، ويحتاج رياضة تعيد ترميز الحركة بمعنى جديد.")

    # 138. التقمّص النفسي
    if any(w in text for w in ["أحسني شخصيات كثيرة", "أعيش دور مو لي"]):
        traits.append("يتقمص أدوارًا غيره، ويحتاج رياضة تعيده لذاته الأصلية دون تمثيل.")

    # 139. الصوت الداخلي الصارم
    if any(w in text for w in ["دايم ألوم نفسي", "في صوت بداخلي يجلدني", "ما أسمح لنفسي أرتاح"]):
        traits.append("يعيش تحت ضغط داخلي قاسٍ، ويحتاج رياضة تسمح له بالرحمة واللطف.")

    # 140. الحنين لما لم يحدث
    if any(w in text for w in ["أشتاق لأشياء ما صارت", "أحسني كنت أقدر أكون شيء ثاني"]):
        traits.append("يشتاق لاحتمالات ضاعت، ويحتاج رياضة تصنع له واقعًا جديدًا بدل الندم.")

    # 141. الرغبة في اختراع الذات من الصفر
    if any(w in text for w in ["أبغى أخترع نفسي", "أبغى شيء ما حد سوّاه", "أخلق نسختي الخاصة"]):
        traits.append("مستعد لبناء هوية جديدة كليًا، وقد تناسبه رياضة جديدة يبتكرها بنفسه.")

    return traits