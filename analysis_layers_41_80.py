def apply_layers_41_80(text):
    traits = []

    # 41. الحنين للماضي
    if any(w in text for w in ["أيام زمان", "كنت أحب", "رجعتني", "أحن", "ذكريات"]):
        traits.append("يرتبط بالماضي عاطفيًا، وقد تناسبه رياضات تستحضر الشعور بالزمن.")

    # 42. التعلّق العاطفي بالشيء
    if any(w in text for w in ["ما قدرت أترك", "متعلق", "صعب أنسى", "أعطيت من قلبي"]):
        traits.append("يتعلق عاطفيًا بتجربته، ويحتاج رياضة تُشبع ارتباطه العميق.")

    # 43. رفض السلطة أو التقييد
    if any(w in text for w in ["أكره القوانين", "ما أحب أحد يفرض", "أبغى أقرر بنفسي"]):
        traits.append("يرفض السيطرة ويفضل رياضات تمنحه الحرية الكاملة.")

    # 44. الحاجة للهوية والانتماء
    if any(w in text for w in ["أدور شي يمثلني", "أبغى أنتمي", "أبغى أحس إن لي مكان"]):
        traits.append("يبحث عن هوية داخل الحركة، ويحتاج رياضة تعكس ذاته.")

    # 45. الاحتياج للتقدير والاعتراف
    if any(w in text for w in ["أبغى أحد يلاحظني", "ودي أحد يشوفني", "أحس إني منسي"]):
        traits.append("يتغذى على التقدير وقد تناسبه رياضات تظهر أثره بوضوح.")

    # 46. الحس القيادي
    if any(w in text for w in ["أحب أوجه", "أشرح", "أقود", "أبغى أكون مدرب"]):
        traits.append("يميل إلى الدور القيادي وقد يزدهر في بيئات فيها إلهام أو تعليم.")

    # 47. الشعور بالتمرد الوجودي
    if any(w in text for w in ["أرفض كل شي", "أبغى أكسر الروتين", "ما أثق بأحد"]):
        traits.append("يحمل تمردًا داخليًا ويحتاج رياضة تحترم فوضاه وتحوله.")

    # 48. الجسد كعقوبة
    if any(w in text for w in ["أجلد نفسي", "أستاهل التعب", "أتعب عشان أرتاح"]):
        traits.append("يتعامل مع الحركة كنوع من التكفير، ويحتاج رياضة توازنه.")

    # 49. فلسفة الذات والتحول
    if any(w in text for w in ["أنا مو زي أول", "قاعد أتغير", "أبغى أكون نسخة جديدة"]):
        traits.append("يرى الرياضة كوسيلة للتحول العميق وليس مجرد نشاط جسدي.")

    # 50. الازدواج النفسي (حُب/كره، سُرعة/بطء)
    if any(w in text for w in ["أحيان أحب، أحيان أكره", "في صراع داخلي", "ما أعرف وش أبغى"]):
        traits.append("يمر بازدواج نفسي، وقد يحتاج رياضة تفتح له مساحة آمنة للتجربة.")

    # 51. التأثر بالمجتمع
    if any(w in text for w in ["الناس تقول", "أهلي أثروا فيني", "صديقي خلاني أجرب"]):
        traits.append("يتأثر بالبيئة وقد يحتاج دعم خارجي لتثبيت هويته الرياضية.")

    # 52. الحس بالفشل المتكرر
    if any(w in text for w in ["جربت كثير وفشلت", "كل مرة أبدأ وأوقف", "ما عمري كملت شي"]):
        traits.append("تراوده مشاعر الفشل ويحتاج رياضة تبني له قصة انتصار خاصة.")

    # 53. الفضول الغامض
    if any(w in text for w in ["مدري ليه بس شدني", "أبغى أجرب شي غريب", "أبغى أكسر النمط"]):
        traits.append("يتحرك بدافع الفضول وقد تناسبه رياضات غير تقليدية.")

    # 54. الحس الوجودي العميق
    if any(w in text for w in ["وش معنى الحياة", "أحسني ضايع", "أدور نفسي"]):
        traits.append("يبحث عن معنى، ويرى في الرياضة أداة للوجود والتعريف بالذات.")

    # 55. التكرار القاتل
    if any(w in text for w in ["كل يوم نفس الشي", "أمل", "ما في جديد"]):
        traits.append("يشعر بالملل من التكرار، ويحتاج رياضة متغيرة دائمًا.")

    # 56. الجسد كهوية
    if any(w in text for w in ["جسمي يمثلني", "أبغى شكلي يوضح من أنا", "الناس تحكم من شكلي"]):
        traits.append("يرى في جسده صورة لهويته، وقد يناسبه نمط رياضي بصري أو بدني دقيق.")

    # 57. التحفّظ العاطفي
    if any(w in text for w in ["ما أتكلم كثير", "أخفي مشاعري", "صعب أشرح"]):
        traits.append("كتماني المشاعر وقد يحتاج رياضة تفتح له مساحة آمنة للتفريغ.")

    # 58. الخضوع للضغط المجتمعي
    if any(w in text for w in ["أتمرن عشان الناس", "أهلي يضغطون", "أحس مجبور"]):
        traits.append("يتأثر بضغوط خارجية ويحتاج استكشاف دافع داخلي حقيقي.")

    # 59. تقديس الألم كقيمة
    if any(w in text for w in ["الألم يعلمني", "بدون ألم ما في تطور", "الألم جزء من الحياة"]):
        traits.append("يرى في الألم نضجًا، وقد يختار رياضات فيها صعود وتحدي ذاتي.")

    # 60. الفوضى العاطفية
    if any(w in text for w in ["مشاعري متقلبة", "أحس بأشياء كثيرة", "أضيع بجواي"]):
        traits.append("يمتلك عمق شعوري غير مستقر، ويحتاج رياضة تُنظم فوضاه لا تقمعها.")

    # 61. الخيال كقوة
    if any(w in text for w in ["أتخيل نفسي", "أتخيل أكون بطل", "أعيش قصص براسي"]):
        traits.append("يمتلك خيالًا حيًا، وقد تناسبه رياضات تحاكي القصص والتمثيل.")

    # 62. الشك الدائم بالنفس
    if any(w in text for w in ["يمكن ما أقدر", "أخاف أجرب", "ما أحس إني قدها"]):
        traits.append("تمنعه الشكوك من الانطلاق، ويحتاج رياضة تعزز ثقته بالخطوة الأولى.")

    # 63. الحس بالاختناق
    if any(w in text for w in ["أحس مخنوق", "ودي أتنفس", "ودي أطلع كل شي"]):
        traits.append("يشعر بالضيق ويحتاج رياضة تحرره جسديًا وعاطفيًا.")

    # 64. التعلق بالمكان
    if any(w in text for w in ["أحب المكان اللي أتمرن فيه", "ما أحب أطلع", "الجو يهمني"]):
        traits.append("يتأثر بالبيئة المكانية، وقد تناسبه رياضات داخلية أو خارجية حسب طبعه.")

    # 65. الانجذاب لما هو نادر
    if any(w in text for w in ["ما يحبونه كثير", "غريب", "ما قد شفت أحد يسويها"]):
        traits.append("ينجذب لما هو نادر وفريد، وقد تناسبه رياضات نادرة أو مبتكرة.")

    # 66. التحدي الذاتي العنيف
    if any(w in text for w in ["أتحدى نفسي", "أكسر حدودي", "ما أرضى بسهولة"]):
        traits.append("يتحدى ذاته باستمرار، ويبحث عن رياضة تتطلب تجاوز النفس.")

    # 67. البحث عن الانفصال عن الجسد
    if any(w in text for w in ["أبغى أنسى جسمي", "أتمرن عشان ما أحس", "ودي أخرج من إحساسي"]):
        traits.append("يمارس الرياضة كوسيلة للانفصال، وقد تناسبه رياضات ذهنية أو استغراقية.")

    # 68. ربط الجسد بالعار أو الخجل
    if any(w in text for w in ["أستحي", "جسمي يحرجني", "أكره شكلي"]):
        traits.append("يحمل مشاعر خجل جسدي، ويحتاج رياضة تبني تصالحًا لا مقارنة.")

    # 69. الحس بالاضطرار لا الرغبة
    if any(w in text for w in ["لازم أتحرك", "ما لي خيار", "غصب"]):
        traits.append("يتحرك من باب الضرورة، وقد يحتاج رياضة تعيد له الرغبة والشغف.")

    # 70. الشعور بالوحدة داخل الجماعة
    if any(w in text for w in ["مع الناس بس حاس وحيد", "أشارك بس ما أحس بشي"]):
        traits.append("يشعر بالانعزال رغم المشاركة، ويحتاج رياضة تُشعره بالاندماج الحقيقي.")

    # 71. الإدمان على الحركة
    if any(w in text for w in ["أتحرك كثير", "ما أقدر أوقف", "أدمن الرياضة"]):
        traits.append("يرتبط بالحركة كإدمان، وقد تناسبه رياضات ذات طابع متجدد.")

    # 72. الميل للطقوس
    if any(w in text for w in ["لازم أبدأ بنفس الطريقة", "عندي طقس", "كل مرة أسوي كذا"]):
        traits.append("يتعلق بالطقوس وقد تناسبه رياضات تعتمد على روتين خاص وطقسي.")

    # 73. الحس بالبطء الوجودي
    if any(w in text for w in ["الوقت واقف", "ما في حركة", "أبغى أبدأ بس ما أقدر"]):
        traits.append("يشعر بثقل الزمن، ويحتاج شرارة حركة تُطلقه.")

    # 74. العيش عبر التحدي
    if any(w in text for w in ["أعيش لما أتحدى", "أبغى أنكسر وأرجع أقوم"]):
        traits.append("يتغذى على التحدي الحقيقي، وقد تناسبه رياضات قاسية وملحمية.")

    # 75. المشاعر المعطّلة
    if any(w in text for w in ["ما أحس بشي", "مخدر", "جامد", "بلا مشاعر"]):
        traits.append("انفصال عن الشعور، ويحتاج رياضة تعيد له الوعي بالجسد والعاطفة.")

    # 76. الرفض المطلق للرياضة التقليدية
    if any(w in text for w in ["أكره الرياضة", "ما أحب التمرين", "الرياضة تزعجني"]):
        traits.append("يحتاج مقاربة مختلفة تمامًا للرياضة، تخرج عن النمط الكلاسيكي.")

    # 77. الحس بغياب الهدف
    if any(w in text for w in ["مدري ليه أسوي كذا", "ما أدري وش الهدف", "كل شي فاضي"]):
        traits.append("يمارس دون هدف، وقد تناسبه رياضة تساعده على الاكتشاف التدريجي.")

    # 78. التعلق بالجمال
    if any(w in text for w in ["أبغى شكلي يتغير", "الجسم مهم", "أبغى أنحف/أضخم"]):
        traits.append("يربط الرياضة بالجمال الظاهري، وقد يحتاج وعي أعمق لتوسيع رؤيته.")

    # 79. الحس الفني الداخلي
    if any(w in text for w in ["أحب الإحساس", "الحركة فن", "ما أهتم بالقوة بقدر الشعور"]):
        traits.append("يرى الرياضة كفن داخلي وشعور، لا كأداء خارجي فقط.")

    # 80. الميل للاندماج في بيئة فريدة
    if any(w in text for w in ["أبغى شي غير الناس", "أبغى أعيش جو مختلف", "أدور تجربة محد يعرفها"]):
        traits.append("يبحث عن بيئة رياضية نادرة تمثله، خارج المألوف.")

    return traits